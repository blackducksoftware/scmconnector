envvars=[method://java.lang.System.getenv()]
		
smtp.address=${envvars->USER}
smtp.from=${envvars->USER}
smtp.to=${envvars->USER}
		
protex.server.name=${envvars->USER}
protex.user.name=${envvars->USER}
protex.password=${envvars->USER}
protex.password.isencrypted=false
protex.run_rapidid=${envvars->USER}
protex.home_directory=${envvars->USER}
total_connectors=1
connector.0.class=${envvars->USER}
connector.0.repositoryURL=${envvars->USER}
connector.0.user=${envvars->USER}
connector.0.password=${envvars->USER}
connector.0.new_project_name=${envvars->USER}
connector.0.root=${envvars->USER}